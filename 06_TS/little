tsc --init生成tsconfig.json

ts-node运行.ts文件
ts-node index.ts

BOM（浏览器对象模型)
DOM 文档对象模型（Document Object Model)

通配符
./* -当前目录中的任何文件。

./** -当前目录和子目录中的任何文件。


{
  "compilerOptions": {
    "module": "commonjs",
    "baseUrl": "./", // types文件夹的相对路径
    "paths": { "*": ["types/*"]}
  }
}
