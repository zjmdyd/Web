<!-- 
给el-table传入span-method方法，传入的方法A的参数是一个对象，
里面包含当前行row、当前列column、当前行号rowIndex、当前列号columnIndex四个属性。
在表格渲染的时候，A方法会自动执行并遍历表格主体部分的每一个单元，
我们需要在方法内部通过条件判定合并部分的起始单元格（并return[合并的行数、合并的列数]）、
以及被合并的单元格（并return [0,0]，即隐藏被合并的单元格）
 -->